<div class="social">
  <ul>
    <% sites.each do |name, url| %>
      <li>
        <%= link_to "#{url}/#{setting["#{name}_handle"]}", title: link_title(name), target: "_blank", rel: "noopener noreferrer" do %>
          <span class="show-for-sr"><%= link_text(name) %></span>
          <span class="icon-<%= name %>" aria-hidden="true"></span>
        <% end %>
      </li>
    <% end %>

    <!-- Removed footer_content_block rendering -->
  </ul>
</div>
