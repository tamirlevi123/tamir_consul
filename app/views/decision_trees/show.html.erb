<%= content_for :head do %>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/treant-js/1.0/Treant.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/treant-js/1.0/vendor/perfect-scrollbar.min.css" />
  <script src="https://cdnjs.cloudflare.com/ajax/libs/raphael/2.3.0/raphael.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/treant-js/1.0/Treant.min.js"></script>
  <style>
    .modal {
      display: none;
      position: fixed;
      z-index: 1000;
      left: 0; top: 0; width: 100%; height: 100%;
      overflow: auto;
      background: rgba(0,0,0,0.4);
    }
    .modal-content {
      background: #fff;
      margin: 10% auto;
      padding: 20px;
      border: 1px solid #888;
      width: 340px;
      border-radius: 8px;
      position: relative;
    }
    .close-modal {
      color: #aaa;
      position: absolute;
      right: 10px;
      top: 10px;
      font-size: 24px;
      font-weight: bold;
      cursor: pointer;
    }
    .speech-bubble-icon {
      display: none;
      margin: 8px auto 0 auto;
      width: 32px;
      height: 32px;
      transform: rotate(180deg);
      cursor: pointer;
      transition: filter 0.2s;
    }
    .selected-node .speech-bubble-icon {
      display: block;
    }
    .speech-bubble-icon:hover {
      filter: brightness(0.8);
    }
    .nodeExample1 {
      min-width: 120px;
      min-height: 40px;
      text-align: center;
      border-radius: 18px;
      background: #f8f9fa;
      border: 2px solid #bdbdbd;
      box-shadow: 0 2px 8px rgba(0,0,0,0.04);
      padding: 10px 16px;
      transition: border 0.2s, box-shadow 0.2s;
      position: relative;
      cursor: pointer;
    }
    .selected-node.nodeExample1 {
      border: 2.5px solid #1976d2;
      box-shadow: 0 4px 16px rgba(25, 118, 210, 0.08);
      background: #e3f2fd;
    }
    .button.tiny { font-size: 12px; margin-top: 8px; }
    .url-preview {
      margin: 0.5em 0 0.5em 0;
      font-size: 0.95em;
      color: #1976d2;
      word-break: break-all;
    }
    .confirm-url-btn {
      margin-top: 0.5em;
      background: #1976d2;
      color: #fff;
      border: none;
      border-radius: 4px;
      padding: 0.3em 1em;
      cursor: pointer;
      font-size: 0.95em;
    }
    .confirm-url-btn:disabled {
      background: #bdbdbd;
      cursor: not-allowed;
    }
  </style>
<% end %>

<div class="row">
  <div class="small-12 column">
    <h1><%= @decision_tree.title %></h1>
    <p><%= @decision_tree.description %></p>

    <% if @tree_json %>
      <div id="tree-simple" style="width:100%; min-height:600px;"></div>
    <% else %>
      <div class="callout warning">
        <p>This decision tree has no nodes yet.</p>
        <% if user_signed_in? %>
          <%= link_to "Add Root Node", new_decision_tree_decision_node_path(@decision_tree), class: "button" %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<% if user_signed_in? %>
<!-- Modal for adding child node -->
<div id="addChildModal" class="modal">
  <div class="modal-content">
    <span class="close-modal" id="closeAddChildModal">&times;</span>
    <h4>Add Child Node</h4>
    <%= form_with url: decision_tree_decision_nodes_path(@decision_tree), method: :post, local: true, id: 'addNodeForm' do |f| %>
      <%= hidden_field_tag :parent_id, nil, id: 'modal_parent_id' %>
      <div>
        <%= f.label :content, "Node Content" %><br>
        <%= f.text_field :content, required: false, name: 'decision_node[content]', id: 'node_content_field' %>
      </div>
      <div style="margin-top: 0.5em;">
        <%= f.label :url, "URL (optional)" %><br>
        <%= f.text_field :url, required: false, name: 'decision_node[url]', id: 'node_url_field' %>
        <div id="urlPreview" class="url-preview" style="display:none;"></div>
        <button type="button" id="confirmUrlBtn" class="confirm-url-btn" style="display:none;">Confirm URL</button>
        <%= hidden_field_tag 'decision_node[url_confirmed]', false, id: 'url_confirmed_field' %>
      </div>
      <div style="margin-top: 1em;">
        <%= f.submit "Add Node", class: "button", id: 'addNodeSubmit', disabled: true %>
      </div>
    <% end %>
  </div>
</div>
<% end %>

<%= content_for :scripts do %>
  <script>
    window.decisionTreeData = {
      railsTree: <%= raw(@tree_json.to_json) %>,
      nodeIds: <%= @decision_tree.decision_nodes.order(:id).pluck(:id).to_json %>,
      userSignedIn: <%= user_signed_in? ? 'true' : 'false' %>
    };
  </script>
<% end %> 